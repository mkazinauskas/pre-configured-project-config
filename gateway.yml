zuul:
  routes:
    api:
      path: /api/**
      serviceId: project-a

eureka:
  client:
    enabled: true
    service-url:
      defaultZone: http://localhost:8761/eureka/

ribbon:
  eureka:
    enabled: false

server:
  port: 8080
